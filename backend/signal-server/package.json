{
  "name": "@humanos/signal-server",
  "version": "0.1.0",
  "private": true,
  "description": "HumanOS signaling server — WebSocket only (no media relay)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
